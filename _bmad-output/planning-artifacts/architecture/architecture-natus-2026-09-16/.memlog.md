---
scope: Website institucional Grupo Natus — Next.js + conteúdo estático + Supabase (condicional)
purpose: build-substrate
altitude: initiative
updated: 2026-09-16T21:47
---

- (version) Next.js 16.3.5 (App Router, Active LTS) — verificado web set/2026
- (version) React 19 (recomendado p/ Next 16); TypeScript 5.x estrito
- (version) Tailwind CSS v4.3 (motor @theme para tokens); Vitest 5.0.1 + Testing Library
- (assumption) supabase-js 2.x + @supabase/ssr para App Router — pin a confirmar [ASSUMPTION]
- (assumption) Starter: create-next-app (App Router, TS, Tailwind, ESLint) [ASSUMPTION]
- (assumption) Provedor de email: Resend via Route Handler [ASSUMPTION]
- (decision) AD-1 Paradigma: layered feature-based no App Router; Server Components padrão, Client só quando necessário. Previne split client/server arbitrário.
- (decision) AD-2 Empreendimento como dado: conteúdo estático em src/content, fonte única via getAllDevelopments/getDevelopmentBySlug; DB só sob necessidade. Previne páginas duplicadas e DB prematuro.
- (decision) AD-3 Integrações externas (WhatsApp, Maps, email) atrás de abstração única em src/lib; nunca inline. Previne hardcoding espalhado.
- (decision) AD-4 Mutação/escrita só via Route Handlers no servidor; forms POST server-side; env server-only; nunca secret no client. Previne vazamento de secret e submissão inconsistente.
- (decision) AD-5 Supabase/RLS: opcional; quando usado RLS obrigatória; nunca alterar RLS/env existentes; client server-side via @supabase/ssr; anon key só p/ leitura pública. Previne bypass de RLS.
- (decision) AD-6 SEO: cada rota é dona de seu Metadata (API do Next); metadata por empreendimento gerada do conteúdo; sitemap/robots na raiz. Previne SEO divergente.
- (decision) AD-7 Estilo só via design tokens (Tailwind v4 @theme); sem cores ad-hoc. Previne drift da marca.
- (decision) AD-8 TDD: testes colocados (*.test.tsx), threshold de cobertura 90% no vitest.config. Previne código sem teste.
- (constraint) Preservar RLS/env/secrets do Supabase existente [ADOPTED] (README §17)
- (constraint) Nunca inventar conteúdo — ausência vira TODO: CONTENT REQUIRED [ADOPTED] (README §27)
- (event) Spine destilado do memlog (Fast path, build-substrate)
- (decision) AD-5 reforçado: na migração para o banco, o banco vira fonte única de verdade (fecha furo adversarial de dois donos do progresso).
- (event) Finalize reviewers: versões verificadas na web (Next 16.3.5, React 19, Tailwind 4.3, Vitest 5.0.1); supabase-js/Resend/starter marcados [ASSUMPTION].
- (event) Story E1.1 concluída: scaffold instalado. Versões reais — Next 16.3.5, React 19.3, TS 6.0.x (spine pinava 5.x → atualizado), Tailwind 4.3.3, Vitest 5.0.1 + RTL 16 + jsdom 29, ESLint 9.39 + eslint-config-next 16.3.5 (flat nativo).
- (decision) Starter: scaffold MANUAL em vez de create-next-app [RESOLVED] — controle fino do vitest.config (coverage thresholds 90, include só código de app) e da estrutura top-level (app/ components/ lib/ ... sem src/) do spine.
- (decision) ESLint flat config importa `eslint-config-next/core-web-vitals` e `/typescript` diretamente (arrays nativos); FlatCompat causava "circular structure to JSON" com ESLint 9 → descartado.
- (event) Gate TDD comprovado: arquivo-fonte sem teste derruba cobertura p/ 25% e o `test:coverage` sai != 0.
- (event) Stories E1.2/1.3/1.4 concluídas: tokens (@theme derivado do logo), shell (Header/MobileNav/Footer), 7 primitivos (components/ui). Cobertura 100/94/100/100.
- (decision) DevelopmentStatus + STATUS_META criados cedo em types/development.ts (Badge precisa) — E2.1 estende com Development/ConstructionProgress.
- (decision) Convenção AD-7 automatizada: styles/no-raw-color.test.ts falha o build se houver cor crua (hex/rgb/hsl) em app|components|features.
- (advisory) Modal.tsx ("use client") recebe prop de função `onClose` → plugin TS do Next emite 71007 (editor-only). Não quebra typecheck/lint/build. Mantido: renomear p/ *Action seria enganoso (não é Server Action); Modal é primitivo consumido por client components.
- (gate) GATE A APROVADA (2026-09-16): theme-showcase validado pelo usuário. Design system fechado; Epic 1 concluída (6/6). Liberado para Epic 2.
- (event) Epic 2 concluída (Home/catálogo/filtros). Epic 3 stories 3.1–3.6 concluídas: template [slug] (galeria/detalhes/localização/CTA/SEO). 16 páginas SSG no build.
- (decision) Imagens: pastas em img/ são atribuídas por empreendimento (Follow Savassi, Gutierrez, Golden Ville). Follow Savassi populado com renders reais para a Gate B. TODO: comprimir fontes (NFR1).
- (decision) lib/maps: embed Google Maps sem API key (q=coord/endereço&output=embed); degrada p/ link quando só há cidade/UF (FR7, AD-3).
- (gate) GATE B: aguardando aprovação do template (Follow Savassi) antes de fechar Epic 3.
- (event) Gate B iteração 1: página de empreendimento enriquecida conforme referência estrutural novolar (só UX, identidade Natus). Novos componentes: DevelopmentHero (full-bleed), DevelopmentAnchorNav (sub-nav âncora sticky), DevelopmentOverview (visão geral + highlights), DevelopmentAmenities (accordion "o que oferece" + download). Modelo estendido: tagline, highlights, amenities, videoUrl, presentationUrl. next/image `priority`→`preload` (deprecação v16). 144 testes.
- (gate) GATE B APROVADA (2026-09-16): template de empreendimento validado (Follow Savassi). Epic 3 concluída (7/7). Liberado para Epic 4.
- (event) Epic 5 concluída (Conversão por Leads). Motor único LeadForm (schema + validação client + honeypot + feedback) alimenta Fale Conosco (/contato), Interesse (na página do empreendimento, slug pré-preenchido) e Negocie seu Terreno (/negocie-seu-terreno). Route Handler /api/leads: validação server, honeypot, rate-limit (lib/rateLimit em memória). lib/email (AD-3/AD-4): Resend [ASSUMPTION] atrás de RESEND_API_KEY+LEADS_EMAIL_TO; sem config → delivered=false + warn (não perde silenciosamente). 193 testes.
- (decision) next event type: React.FormEvent/FormEvent deprecados no @types/react 19 → usar SyntheticEvent<HTMLFormElement>.
- (todo) 🔔 5.4 campos do terreno a confirmar; 🔔 5.5 provedor de email + secret (docs/CONTENT-GAPS.md §4).
- (event) Epics 6 e 7 concluídas. Institucionais (Quem Somos/Engenharia, conteúdo 🔔 TODO). SEO site-wide: app/sitemap.ts, app/robots.ts (bloqueia theme-showcase e /api), metadataBase+OG no layout, JSON-LD (Organization no layout, Residence por empreendimento). A11y: skip-link + landmarks. Motion: prefers-reduced-motion global. lib/site.SITE_URL (natusgrupo.com.br, TODO confirmar domínio).
- (milestone) PLANO ATÉ EPIC 7 COMPLETO: 32/35 stories. Build 23 páginas (estático/SSG; /api/leads dinâmico). 205 testes, cobertura ~99/94/99/99. Epic 8 (Supabase/Admin, Gate C) fica condicional/fora do plano.
- (event) Iniciativa de Modernização de Frontend implementada. Adoção shadcn/Radix: Dialog, Accordion, Tabs, NavigationMenu, **Progress** (@radix-ui/react-progress) e **Select** (@radix-ui/react-select). Tipografia Fraunces+Inter (next/font); ícones lucide; motion via `motion` (framer-motion). Build 24 páginas, 224 testes.
- (decision) Forms migrados p/ **react-hook-form + zod** (@hookform/resolvers). LeadForm monta um schema zod dinâmico a partir do array `fields` (refine p/ obrigatório/email/telefone reutilizando lib/validators); honeypot curto-circuita ANTES da validação; payload e /api/leads (honeypot+rate-limit) inalterados. Input/Textarea/Select passaram a `ComponentPropsWithRef` + forward de `ref` (React 19) p/ o register uncontrolled.
- (decision) **Select**: Radix (`SelectField`) SÓ nos filtros do catálogo (UX de painel estilizado); `<select>` NATIVO mantido no lead form por robustez (mobile/no-JS) — reafirma a nota do plano A4. Sentinela "all" no catálogo (Radix Select não aceita value="").
- (decision) **Progress**: Radix Progress somente-leitura (barras rotuladas), NÃO sliders — a referência do site atual usa barras por etapa. Fill em gradiente de marca (from-brand→brand-strong); % em texto grande usa brand-strong (nude-600 #a67a5e passa AA p/ texto grande ~3.9:1).
- (decision) Galeria: imagem em destaque com **lupa (magnifier)** no hover — lente circular com background-image ampliado seguindo o cursor; gated por prefers-reduced-motion e pointerType mouse. Sem `priority` no next/image do destaque (abaixo da dobra; protege o LCP do hero).
- (advisory) zodResolver de schema dinâmico: input genérico resolve p/ `{[x]:unknown}`; cast direcionado `as Resolver<LeadValues>` no useForm (output é string, benigno).
- (decision) Revisão de design do diff resolveu achados de contraste (WCAG AA): `--color-muted-foreground` stone-400→stone-600 (≈3:1→≈6:1) e novo `--color-status-error` (#b3261e, ≈6.3:1) p/ erros de form (antes reusava o âmbar de "em construção", que reprovava e lê como aviso). `--color-destructive` repontado p/ o erro. AD-7 mantido (cor só em .css).
- (decision) Galeria: substituída a lupa por **hover-zoom** (zoom-and-pan via `motion` scale + transform-origin no cursor) — melhor com next/image (sem background-image duplicado) e mesma linguagem de motion do site. ProgressBar virou client e anima o fill on-view (scaleX 0→valor, origin-left), respeitando reduced-motion.
